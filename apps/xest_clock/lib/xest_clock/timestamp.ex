defmodule XestClock.Timestamp do
  #  alias XestClock.Time

  #  def with_previous(%Time.Stamp{} = recent, %Time.Stamp{} = past) do
  #    %{recent | ts: recent.ts |> XestClock.TimeValue.with_derivatives_from(past.ts)}
  #  end
end
